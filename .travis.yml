language: java
jdk:
  - oraclejdk8
dist: trusty
env:
  - NODE_VERSION="8"
addons:
  sonarcloud:
    organization: "orkunkocyigit-github"
before_install:
  - "nvm install $NODE_VERSION"
  - "npm update -g npm"
script:
  - npm test
  - sonar-scanner
  - npm run production-build
